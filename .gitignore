.env
backend/.env

# Python virtual environments
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
frontend/joyverse/build/

# Dev screenshots (docs only — keep out of the repo)
workingScreenshots/

# Abandoned / large ML model binaries (kept on disk, not in git)
*.pth
*.tflite

# Transient NTFS/FUSE artifacts (open handles to deleted files)
.fuse_hidden*
